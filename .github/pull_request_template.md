## What changed

-

## Why

-

## Test plan

- [ ] `npm run lint` / `npx tsc --noEmit` / `npm run build` pass locally
- [ ] Checked in Vercel Preview deploy (link auto-posted by Vercel bot below)
- [ ] i18n: new strings added to all 7 locale files (`ko`/`en` real, others stubbed + logged in `i18n-todo.md`)
- [ ] No hardcoded hex / spacing / `text-[Npx]` — tokens only
- [ ] No secrets committed (gitleaks clean)
